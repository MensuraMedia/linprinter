# 2026-09-22 — 0.2.0 first release

**What:** the full 0.1 + 0.2 roadmap (see changelog.md), plus Setup and test on the Printers page after the
user asked that LinPrinter also cover printer configuration, testing and other printer features.

**Files:** src/backends/{ipp,test_printer,backend_base,backend_ipp,backend_cups,backend_pdf,usb_probe}.py,
src/modules/{manager_print,manager_render,manager_status,manager_testpage,manager_documents,manager_settings}.py,
src/pages/page_{print,preview,queue,recent,printers,settings,about}.py, src/features/feature_{ink_alerts,profiles}.py,
src/main.py, src/ui/*, install.sh, app.json, LICENSE, docs/*, tests/*.

**Fixes found while building:**
- ipptool "Bad HTTP version (1.0)" → TestPrinter handler uses HTTP/1.1.
- Duplicate printer-uri-supported from the fixture → filtered.
- dateTime values could not be encoded → DATETIME encoding added.
- rangeOfInteger / resolution wrapped twice → encoder fixed.
- describe() treated the reason "none" as a warning → skipped.
- Logging imported LinScanner's parser_sane → own redaction (hex serials, SN: in device IDs, home path).
- TestPrinter did not create a given spool folder → created.

**Verified:** 32 tests pass; walkthrough with the test printer (screenshots in docs/screenshots);
real TR150 detected read-only (both methods, ink, defaults, maintenance links); nothing printed on hardware.

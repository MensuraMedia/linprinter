# 2026-09-22 — 0.2.1 installer package, screenshots, README

**Request:** "works well. update all documentation. update handoff, ensure the git has installer package
and installer script. ensure there are proper screenshots and robust readme. run local backup. commit and push."

**What:**
- tools/build-deb.sh → dist/linprinter_0.2.1_all.deb + dist/SHA256SUMS (committed).
- install.sh: `--package` (checksum-verified system install, removes duplicate per-user entry),
  `--uninstall` also removes the package, uninstall summary wording, LINPRINTER_ALLOW_ROOT for containers.
- tools/walkthrough.py rewritten: sandbox home, sample documents, 17 screenshots.
- README rewritten (screenshot tour, two install ways, tips, troubleshooting); docs updated.
- UI polish: profile row position, ~ paths, Recent column widths, progress reset, PDF capability wording.
- tests/test_packaging.py (3 tests) → 35 tests.

**Root causes of the polish fixes:**
- The profile row was reordered to index 0 of a card whose first child is its title.
- Paths were shown raw in the PDF summary, the PDF result and the Printers connection line.
- The Recent fixed column widths summed wider than the window.

**Verified:**
- black, pyflakes and pytest (35 passed).
- The walkthrough.
- The host installer: all OK.
- ubuntu:24.04 --network none: dependencies from the pool, the .deb installs and runs; install.sh
  default, --package (twice) and --uninstall behave. CUPS FAILs there only because there's no systemd.
